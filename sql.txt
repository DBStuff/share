CREATE TABLE USERS (username varchar(255) PRIMARY KEY, password varchar(255) NOT NULL);

CREATE TABLE UPDATES (id int PRIMARY KEY, query varchar(255) NOT NULL);

CREATE TRIGGER monitorSightnings BEFORE INSERT, UPDATE, DELETE ON SIGHTINGS
BEGIN
    INSERT INTO UPDATES (query) VALUES (NEW.);
END;